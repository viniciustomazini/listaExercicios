        class Pokemon {
            constructor(id, name, type, types, photo, stats) {
                this.id = id;
                this.name = name;
                this.type = type;
                this.types = types;
                this.photo = photo;
                this.stats = stats; // Array of objects { name, value }
            }
        }